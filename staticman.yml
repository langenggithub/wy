comments:
  allowedFields: ["name", "email", "message"]
  branch: "main"
  format: "json"
  moderation: true
  requiredFields: ["name", "message"]
  generatedFields:
    date:
      type: date
      options:
        format: "iso8601"
